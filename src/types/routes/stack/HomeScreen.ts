export interface HomeScreenParams {
  queueDate: string;
  branchName: string;
  currentQueue: number;
  lastQueue: number;
  needs: string;
  userId: number;
  branchId: number;
  queueNumber: number;
}
